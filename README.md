# Field Sales

Mobile-first PWA prototype for a fertilizer distributor's field sales team: daily check-in, offline visit
& online engagement reporting, customer database, journey planner, funnel dashboard,
Toko Tani bulk ordering (loco/franco), and price intelligence.

Built with React + Vite + TypeScript + Tailwind. Data lives in `localStorage` with a
generated demo dataset — no backend required to run or explore it.

## Run locally

```bash
npm install
npm run dev
```

## Demo login

- Rep: `081234500001`, `081234500002`, or `081234500003`
- Ops manager: `081234599999`
- OTP code (any number): `123456`

## Deploy

Pushing to `main` builds and deploys automatically to GitHub Pages via
`.github/workflows/deploy.yml`.
